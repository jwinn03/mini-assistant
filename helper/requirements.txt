# Core (always required)
websockets>=12

# ASR mode (not needed for --echo transport tests)
numpy
faster-whisper

# GPU ASR (Phase 9, optional): CUDA-12-series runtime DLLs for ctranslate2.
# Needed even with a system CUDA 13 toolkit — ctranslate2 links the
# cublas64_12 / cudnn 9 DLL names. Harmless to skip on CPU-only machines
# (server falls back to CPU automatically with --device auto).
nvidia-cublas-cu12
nvidia-cudnn-cu12

# LLM relay (talks to Ollama or any OpenAI-compatible endpoint)
requests

# TTS (Phase 10): resamples Piper's output to the 16 kHz wire format.
# The Piper binary + voice live under helper/piper/ (see README, not pip).
soxr
